/*
 * Homework: 09_namespaces.cpp
 *
 * Complete the exercises below based on the concepts from 09_namespaces.cpp
 * in the classwork folder.
 *
 * Instructions:
 * 1. Read the corresponding classwork file first
 * 2. Implement the solutions below
 * 3. Compile: g++ -std=c++17 -Wall -Wextra 09_namespaces.cpp -o homework
 * 4. Test your solutions
 */

#include <iostream>

/*
 * TRY THIS:
 * 1. Create a namespace 'geometry' with functions for rectangle area and perimeter
2. Create nested namespaces: graphics::d2 and graphics::d3 with different shapes
3. What happens if two namespaces have functions with same name?
4. Why is "using namespace std;" considered bad practice in large projects?
5. Create a namespace for GPU memory operations (allocate, free, copy)

EXAMPLE STRUCTURE:
namespace myapp {
    namespace gpu {
        void init_device();
        void allocate_memory();
    }
    namespace cpu {
        void process_data();
    }
}

REAL CUDA EXAMPLE:
#include <cuda/std/complex>
using cuda::std::complex;  // Complex numbers on GPU
 */

int main() {
    std::cout << "Homework: 09_namespaces\n";
    std::cout << "Implement the exercises above\n";

    // Your code here

    return 0;
}
