/*
 * Homework: 07_enums.cpp
 *
 * Complete the exercises below based on the concepts from 07_enums.cpp
 * in the classwork folder.
 *
 * Instructions:
 * 1. Read the corresponding classwork file first
 * 2. Implement the solutions below
 * 3. Compile: g++ -std=c++17 -Wall -Wextra 07_enums.cpp -o homework
 * 4. Test your solutions
 */

#include <iostream>

/*
 * TRY THIS:
 * 1. Create an enum class for days of the week
2. Write a function that takes a day enum and returns if it's a weekend
3. Create an enum for GPU execution modes (synchronous, asynchronous, stream)
4. What happens if you try to assign an enum class to int without casting?
5. Create an enum with values that are powers of 2 (for bitwise flags)

EXAMPLE:
enum class MemoryOperation {
    ALLOCATE,
    COPY,
    FREE
};

CUDA EXAMPLE YOU'LL SEE LATER:
enum cudaMemcpyKind {
    cudaMemcpyHostToDevice,
    cudaMemcpyDeviceToHost,
    cudaMemcpyDeviceToDevice
};
 */

int main() {
    std::cout << "Homework: 07_enums\n";
    std::cout << "Implement the exercises above\n";

    // Your code here

    return 0;
}
