# Phase 6: PyTorch Dependencies
#
# NOTE: Do NOT use this file directly for installation!
# Use Makefile targets instead to ensure correct CUDA version:
#   make install-cpu    - For CPU-only machines
#   make install-gpu    - For GPU machines with CUDA
#
# The Makefile will install PyTorch separately with the correct
# CUDA version, then install other dependencies from requirements-base.txt

# Core PyTorch
# Installed separately by Makefile to ensure CUDA compatibility
# Minimum versions:
#   torch>=2.1.0
#   torchvision>=0.16.0
#   torchaudio>=2.1.0
#
# CUDA Compatibility:
#   PyTorch 2.1+: Compatible with CUDA 11.8, 12.1+
#   Auto-detects system CUDA and installs compatible version
#   Includes CUDA runtime libraries (no separate CUDA install needed)

# Training & Monitoring
tensorboard>=2.15.0
pytorch-lightning>=2.1.0

# Computer Vision
timm>=0.9.0  # PyTorch Image Models
albumentations>=1.3.0  # Data augmentation

# NLP (Optional)
transformers>=4.35.0
tokenizers>=0.15.0

# Model Export
onnx>=1.15.0
onnxruntime>=1.16.0

# Utilities
tqdm>=4.66.0
pillow>=10.1.0

# Data Science (from Phase 5)
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# Jupyter Support
jupyter>=1.0.0
ipykernel>=6.25.0
